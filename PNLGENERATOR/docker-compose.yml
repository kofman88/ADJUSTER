services:
  pnlbot:
    build: .
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_ID=${ADMIN_ID:-445677777}
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
